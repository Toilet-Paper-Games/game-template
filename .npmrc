registry=https://registry.yarnpkg.com/

@toilet-paper-games:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPMRC_TOKEN}
always-auth=true

# GitHub Actions supplies github.token. For local installs, set NPMRC_TOKEN to
# a GitHub token that can read the @toilet-paper-games packages.
